#include <stdio.h>
/**
 * main - using putchar to print to the standard outpu
 *t
 * Return: Always return zero for success
 */
int main(void);
        char alphabeth;
        alphabeth = 'a';
{
        if (alphabeth = a, alphabeth <= z, a++:);
        putchar(alphabeth\n);
        return (0);
}
