#include <stdio.h>
/**
 * main - using putchar to print to the standard o *t
 * Return: Always return zero for success
 */
int main(void)
{
	int num;

	for (num = 01; num < 90; num++)
	{
		putchar(num);
	{
		putchar(',');
		putchar(' ');
	}
	}
	putchar('\n');
	return (0);
}
