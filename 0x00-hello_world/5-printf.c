#include <stdio.h>
/**
 * main - using printf to print to the standard o
 * Return: always return zero for success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
