#include "main.h"
/**
 *_abs - describing the function int and use to print
 *absolute integer
 *
 *Return: return i if i is greater than or equal to z
 *ero else return i*-1
 *
 * @i: variable used in the function
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	
		return (i * -1);	
}
