#include "main.h"
/**
 *add - using the funct for addition of 2 integers
 *
 *Return: Always zero for success
 *@y: num being added
 *@x: num being added
 */
int add(int y, int x)
{
	return (y + x);
}

